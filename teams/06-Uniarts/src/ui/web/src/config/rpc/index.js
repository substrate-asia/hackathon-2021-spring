import uart from "./uart";

export default {
    typesSpec: {
        // uart,
    },
    types: {
        ...uart,
    },
    typesChain: {
        // uart,
    },
    typesBundle: {
        // uart,
    },
};
